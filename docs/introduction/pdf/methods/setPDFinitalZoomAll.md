When on, the initial scaling of a PDF will be zoomed to fit.

### Syntax

```typescript
RxCore.setPDFinitalZoomAll(onoff)
```

### Parameters

- `onoff`: **boolean** — `true` = on, `false` = off.

### Returns

- **NA** — This method does not return a value.