Turn on/off fraction notation for markup measurement objects when using Imperial measurement units.

### Syntax

```typescript
RxCore.useFraction(onOff)
```

### Parameters

- `onOff`: **boolean** — `true` = on, `false` = off.

### Returns

- **NA** — This method does not return a value.