Returns `true` if read-only mode is enabled.

### Syntax

```typescript
RxCore.getReadOnly()
```

### Parameters

- **None**

### Returns

- **boolean** — `true` if read-only mode is on, otherwise `false`.