Turns monochrome display on/off for CAD and other vector-based formats.

### Syntax

```typescript
RxCore.setMonoChrome(onOff)
```

### Parameters

- `onOff`: **boolean** — `true` = monochrome on, `false` = monochrome off.

### Returns

- **NA** — This method does not return a value.